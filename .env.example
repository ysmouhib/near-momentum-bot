# Copy to .env and fill in. Testnet keys: https://testnet.binance.vision
# These are read from the environment only; never commit a real .env.
BINANCE_API_KEY=your_key_here
BINANCE_API_SECRET=your_secret_here
